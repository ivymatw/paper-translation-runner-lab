# Validation Artifacts

This directory stores compact machine-readable artifacts from the five-paper validation cycle.

Included:
- `manifests/` — final run manifests for the five full-document runs
- `error-logs/` — error/degraded-page logs when present

Deliberately excluded:
- generated PDFs
- large per-page outputs
- raw caches
- heavyweight intermediate work directories
